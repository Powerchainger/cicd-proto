name: main

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  
env:
  RELEASE_BRANCH: 'refs/heads/main'
  PUSH_AS_LATEST: true
  
jobs:
  # dotnet_build_and_test:
  #   uses: Powerchainger/infrastructure-shared/.github/workflows/dotnet_build.yml@main
  #   with: 
  #     dotnet_version: 6.0.x
  # docker_build_and_test:
  #   strategy:
  #     matrix:
  #       application: [cicd-test]
  #   uses: Powerchainger/infrastructure-shared/.github/workflows/docker_build.yml@main
  #   with:
  #     file: ./DevOps/dockerfile
  #     context: .
  #     push:  ${{ github.event_name != 'pull_request' }}
  #     application: ${{matrix.application}}
  #     push_as_latest: true
  # show:
  #   name: "show image tags: ${{ needs.docker_build_and_test.outputs.image_tags }}"
  #   runs-on: ubuntu-latest
  #   needs: [docker_build_and_test, dotnet_build_and_test]
  #   steps:
  #     - run: echo "${{ needs.docker_build_and_test.outputs.image_tags }}"
  deploy:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@v1

    - name: 'Deploy'
      uses: 'deliverybot/helm@v1'
      with:
        release: 'test-r'
        namespace: ${{ vars.KUBE_CONFIG }}
        chart: './DevOps/Helm/test'
        token: '${{ github.token }}'
        values: |
          TestKey:  ${{ vars.KUBE_CONFIG }}
        value-files: >-
          [
            "values.yaml"
          ]
      env:
        KUBECONFIG_FILE: '${{ secrets.KUBE_CONFIG }}'
